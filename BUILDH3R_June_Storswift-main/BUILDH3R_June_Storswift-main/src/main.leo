program umut_calc_buildh3r_657682b8.aleo {
    transition sum(public a: u32, b: u32) -> u32 {
        return a + b;
    }
    transition subtract(public a: u32, b: u32) -> u32 {
        return a - b;
    }
    transition multiply(public a: u32, b: u32) -> u32 {
        return a * b;
    }
    transition divide(public a: u32, b: u32) -> u32 {
        return a / b;
    }
} 
